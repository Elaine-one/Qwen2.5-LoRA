# --- 模型与数据获取 ---
modelscope>=1.15.0       # 用于 download_dataset.py 和 download_model.py

# --- 微调框架与推理核心 ---
# 注意：llamafactory 依赖较多，建议克隆项目后使用 pip install -e . 安装
# 这里列出其运行所需的核心第三方库
torch>=2.1.2             # 算力核心
transformers>=4.41.2     # 模型加载与处理
peft>=0.11.1             # LoRA 微调核心库
bitsandbytes>=0.43.1     # QLoRA 4-bit 量化支持
accelerate>=0.30.1       # 分布式训练与硬件加速

# --- 数据处理与工具 ---
pandas                   # 数据清洗辅助
sentencepiece            # 分词器支持
protobuf                 # 协议支持